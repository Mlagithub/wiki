created: 20260824081556743
modified: 20260824081743825
tags: 
title: 前后端类型安全自动化方案
type: text/vnd.tiddlywiki

“契约优先”（Contract-First）的前后端类型安全自动化方案。核心目标是彻底消除手写 TypeScript 类型带来的不一致风险，确保前端类型与后端接口定义始终保持绝对同步。

拆解为三个层面深度分析：

1. 操作流程（做了什么）

* 导出契约：通过 curl 命令从后端服务（本地 8000 端口）获取 OpenAPI 规范的 JSON 文件，并将其保存到工程目录下的 engineering/api-schema.json 中。这个文件是后端接口的“蓝图”。

* 生成类型：进入 web 目录，使用 openapi-typescript 工具，将上一步的 JSON 文件编译成 TypeScript 的类型定义文件（src/api/schema.d.ts）。这一步将后端的 Java/Python/Go 数据结构，自动化地“翻译”成了前端的 TS 类型（如 interface 和 type）。

2. 核心约束（编码规范）

* 禁止手写重复类型：要求开发者手写的 web/src/api/*.ts（如 axios 请求封装或具体 API 调用函数）中，只能引用（import）schema.d.ts 里自动生成的类型。

* 杜绝“魔改”：严禁在前端手动定义类似 interface User { ... } 的代码。这样做是为了确保前端使用的数据类型是官方认证的副本，而不是开发人员凭感觉“猜”出来的，从而从源头杜绝字段拼写错误、类型缺失或后端改动后前端忘记修改的问题。

3. CI 机制（质量防线）

* Diff 漂移检查：在持续集成（CI）流水线中，会对比当前 schema.d.ts 文件与重新生成一遍的文件是否有差异。

* 阻断性保护：如果后端 OpenAPI 契约发生了更新（例如新增字段或修改类型），但开发者忘记执行生成命令并提交新的 schema.d.ts，CI 检查就会失败（报错）。这强制要求“契约变更”必须伴随“类型文件的更新”一起提交，防止代码仓库中的类型定义与后端实际接口出现“漂移”（Drift）。

这套方案把生成类型变成了像 npm install 一样的日常必做操作，并利用 CI 当作“监工”。它牺牲了手动编码的“灵活性”，换来了前后端联调时极高的“稳定性”和“安全感”，非常适合中大型项目或多分支并行开发的场景。